# Settings for Backend (on Cloud Run).
# See https://firebase.google.com/docs/app-hosting/configure#cloud-run
runConfig:
  runCommand: pnpm start:webapp
  minInstances: 0
  # maxInstances: 100
  # concurrency: 80
  # cpu: 1
  # memoryMiB: 512

# Environment variables and secrets.
env:
  # Configure environment variables.
  # See https://firebase.google.com/docs/app-hosting/configure#user-defined-environment
  
  # Override the default build command to use our monorepo build script
  - variable: GOOGLE_NODE_RUN_SCRIPTS
    value: build:webapp
    availability:
      - BUILD

  # Override the default start command to use our monorepo start script  
  - variable: GOOGLE_NODE_START_SCRIPT
    value: start:webapp
    availability:
      - RUNTIME

  # Set NODE_ENV to production for optimized builds
  # Firebase App Hosting is purely for building deployable assets
  # Testing and linting are handled in separate CI pipeline (GitHub Actions)
  # so we don't need devDependencies during build/runtime
  - variable: NODE_ENV
    value: production
    availability:
      - BUILD
      - RUNTIME

  # Grant access to secrets in Cloud Secret Manager.
  # See https://firebase.google.com/docs/app-hosting/configure#secret-parameters
  # - variable: MY_SECRET
  #   secret: mySecretRef

