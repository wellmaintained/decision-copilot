# Development environment configuration for webapp
# Only contains values that differ from .env defaults
# Safe to commit - contains no real secrets

# Firebase emulator configuration (development only)
FIREBASE_AUTH_EMULATOR_HOST="127.0.0.1:9099"
FIREBASE_FIRESTORE_EMULATOR_HOST="127.0.0.1:8080"
FIREBASE_STORAGE_EMULATOR_HOST="127.0.0.1:9199"
FIREBASE_FUNCTIONS_EMULATOR_HOST="127.0.0.1:5001"
FIRESTORE_EMULATOR_HOST="127.0.0.1:8080"

# Auth debug logging level for Phase 3 investigation
NEXT_PUBLIC_AUTH_LOG_LEVEL=debug